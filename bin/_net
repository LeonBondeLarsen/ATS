# Setup local machine
sudo sysctl -w net.ipv4.ip_forward=1
sudo iptables -t nat -A POSTROUTING -o wlan0 -j MASQUERADE

# Setup remote machine
send_ssh "sudo route add default gw 192.168.10.247 dev eth0"
send_ssh "sudo sh -c 'echo nameserver 8.8.8.8 >> /etc/resolv.conf'"

