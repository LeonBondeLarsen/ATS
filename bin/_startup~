#!/bin/bash

source _env
source _functions

# Transfer latest vbersion of script to execute
scp ${_STARTUP_SCRIPT} ${_REMOTE_USER}@${_REMOTE_HOST}.local:${_REMOTE_WORKSPACE}


send_command '/home/pi/_stream' 'Enter'



